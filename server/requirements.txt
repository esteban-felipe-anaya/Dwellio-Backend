# --- Web framework ---
fastapi>=0.115,<0.116
uvicorn[standard]>=0.32
gunicorn>=22.0

# --- Database / ORM / migrations ---
SQLAlchemy[asyncio]>=2.0.35
alembic>=1.13
greenlet>=3.1
aiosqlite>=0.20          # zero-install SQLite fallback
# PostgreSQL driver (documented production target). psycopg v3 ships binary
# wheels for all supported Pythons (incl. 3.14) and works with SQLAlchemy's
# async engine via `postgresql+psycopg://`. asyncpg is an alternative where a
# wheel exists for your Python: `pip install asyncpg` + use postgresql+asyncpg.
psycopg[binary]>=3.2

# --- Validation / settings ---
pydantic>=2.9
pydantic-settings>=2.5
email-validator>=2.2

# --- Auth / security ---
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7.4
bcrypt>=4.2              # used directly for hashing (passlib-compatible)

# --- Uploads ---
python-multipart>=0.0.12

# --- HTTP / tests ---
httpx>=0.27
pytest>=8.3
pytest-asyncio>=0.24

# --- Lint / format ---
ruff>=0.6
black>=24.8
